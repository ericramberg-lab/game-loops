import ContactPage from "./ContactPage";

export const metadata = {
  title: "Contact · Game Loops",
  description:
    "Bug reports, mechanic ideas, licensing questions — a real human reads every message.",
};

export default function Page() {
  return <ContactPage />;
}
